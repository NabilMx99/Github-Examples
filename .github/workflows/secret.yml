name: Secrets Workflow

on:
  push:
    branches:
      - main

jobs:
  my-job:
    runs-on: ubuntu-latest

    steps:
      - name: Read Secret
        run: |
          echo "API_KEY=$API_KEY"
        env:
          API_KEY: ${{ secrets.API_KEY }}
